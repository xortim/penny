# Create PR Skill
1. Run `git diff main --stat` to summarize changes
2. Create PR with `gh pr create --base main --fill`
3. If merge conflicts exist, list them and ask user before resolving
4. Never force-merge without user approval
